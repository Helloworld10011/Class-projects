#import tensorflow_probability as tfp
#import tensorflow as tf
#x= tfp.distributions.Categorical(probs=[0.7, 0.1, 0.2]).log_prob(0)
#print(tf.random.categorical(logits=[0.7, 0.1, 0.2], num_samples= 1))

print(1//5)